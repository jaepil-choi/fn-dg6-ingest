"""
Layout definitions sub-package for fn-dg6-ingest.

Contains YAML files that define cell-coordinate mappings for each
known DataGuide 6 export format. The loader module (layouts.py in
the parent package) reads these files at runtime.
"""
